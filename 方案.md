# 神牛 X3 Pro 桌面控制台 — 技术方案（v1）

> 制定：GPT-6 Astra（meta-theory 治理路线，Critical/Fetch/Thinking 已完成）
> 日期：2026-09-22
> 状态：待执行（执行者：Gemini 3.8 Flash）

## 1. 背景与目标

用户是摄影师，工作流为 Capture One 联机拍摄 + 相机热靴上的 Godox X3 Pro 引闪器控制闪光灯。
痛点：每次测试调整闪光功率都要走到相机旁操作引闪器，再回桌面按快门，来回打断节奏。

目标：macOS 桌面应用，蓝牙直连 X3 Pro，在桌面远程调整各组（A/B/C/D/E）闪光功率，
形态参考用户已有的 skill router（本地小工具，UI 简洁，打开即用）。

非目标（本轮不做）：
- 不改 Capture One、不接管快门触发（快门仍由相机/引闪器链路负责）
- 不做手机 App、不做云端、不上架分发
- 不替代引闪器的 2.4GHz 引闪链路（蓝牙只负责调参）

## 2. 可行性依据（GitHub / 公开资料调研结论）

调研结论：**没有现成的桌面控制 Godox 引闪器项目**，但以下积木证明路线可行并可直接复用：

| 证据 | 说明 | 用途 |
|---|---|---|
| Godox Flash App（iOS/Android） | X3 Pro 蓝牙可与手机 App 配对，App 端可远程调功率/组别/频道 | 证明 BLE 控制通道真实存在，也是抓包逆向的操作源 |
| binary-person/ha-godox-mesh | Python/bleak 完整逆向 Godox 蓝牙 mesh 协议，覆盖 186 款灯，含 reverse-artifacts/ 抓包资产与 tools/ | 协议 framing、bleak 连接、逆向方法论的直接参考 |
| JaelynWuJionglin/FDSMeshLibDemo | Godox 官方 Android Mesh SDK demo（GodoxMeshLib + LsTiBleMeshLib，AAR） | 帧构造逻辑的权威参考，必要时 jadx 反编译 |
| mildsunrise/godox-flash-radio | 引闪器到灯头 2.4GHz 射频协议文档 | 备选路线（SDR 旁路）证据，本轮不采用 |

核心未知（执行第一阶段必须回答）：
X3 Pro 的蓝牙是 Godox mesh 同族协议，还是独立的点对点 BLE 协议？是否带配对加密 / network key 分发？

## 3. 技术路线（已选定：BLE 直连桌面 App）

Mac 通过 BLE 直连 X3 Pro，复刻 Godox Flash App 的调参指令。
降级路线（若协议加密无法逆向）：官方 Android Mesh SDK 跑模拟器/旧手机做局域网桥，桌面 HTTP 控制。
不采用 SDR 射频旁路（工程量最大、需 RF 硬件、与引闪器状态脱节）。

## 4. 阶段划分与产出

### P0 侦察（半天到 1 天）
- 用 Python bleak 在 Mac 上扫描 X3 Pro：记录设备名、MAC/UUID、广播数据、全部 service/characteristic UUID 及属性
- 对照 ha-godox-mesh 的协议特征（UUID、mesh radio 型号 LK8620/LK8720/LK8728B）判断是否同族
- 产出：docs/p0-recon.md 侦察记录 + 路线判定（直连可行 / 转 SDK 桥接）

### P1 协议逆向（核心风险阶段）
- 首选（有安卓手机）：开发者选项开启 HCI snoop log，用 Godox Flash App 逐项操作
  （A 组功率 1/128 到 1/1 逐档、切换组、TTL/M 切换、造型灯开关、试闪），Wireshark 解析，建立「操作到 BLE 帧」映射表
- 备选（仅 iPhone）：macOS PacketLogger（Additional Tools for Xcode）抓包，或安卓模拟器跑 App 抓虚拟 HCI
- 必须弄清：连接/配对/鉴权流程、帧结构（header/命令字/参数/校验）、功率值编码（1/128 到 1/1 的映射表）、是否需要先订阅 notify
- 产出：docs/protocol.md 协议文档 + scripts/probe.py 最小验证脚本（bleak 发一帧调 A 组功率，引闪器屏幕/灯头实测变化）

### P2 桌面 App（MVP）
- 技术栈：Python 3 + bleak + FastAPI + 单页 HTML（本地 http://127.0.0.1，skill router 同形态）；依赖尽量少
- 功能 MVP：
  - 扫描并连接 X3 Pro（显示连接状态灯）
  - A 到 E 组功率滑块（1/128 到 1/1，按档步进）+ TTL/M 模式切换
  - 试闪按钮、一键全部复位
  - 断线自动重连
- 注意：X3 Pro 蓝牙大概率同时只允许一个主机连接——桌面连上时手机 App 会断开，属预期行为，需在 UI 提示

### P3 实拍联测与打磨
- Capture One 联机实拍：桌面调功率、按快门、确认曝光变化即时生效
- 稳定性：连续拍摄 50 张不掉线；调参延迟测量（目标小于 300ms 体感即时）
- 可选增强：全局快捷键 / Stream Deck 集成 / 菜单栏常驻

## 5. 风险与缓解

| 风险 | 缓解 |
|---|---|
| R1 X3 Pro 非 mesh 协议，ha-godox-mesh 不能直接复用 | P0 先判定；不同族则以 HCI 抓包 + 官方 SDK AAR（jadx 反编译）为准 |
| R2 配对加密 / network key 分发复杂 | P0 判定后若过复杂，降级到官方 SDK 桥接路线，不硬啃 |
| R3 引闪器固件升级改变协议 | protocol.md 记录固件版本；协议层与 UI 分层，便于适配 |
| R4 桌面 BLE 干扰引闪链路 | 蓝牙（调参）与 2.4GHz（引闪）是独立通道，理论无干扰，P3 实测确认 |

## 6. 里程碑验收（每个都要有实证，不以脚本退出码为准）

- M1：扫描到 X3 Pro 并枚举出特征值（附截图/日志）
- M2：probe.py 调 A 组功率，引闪器屏幕或灯头实测变化（附照片或视频）
- M3：桌面 UI 完整调参流程走通（录屏）
- M4：Capture One 实拍联测通过（样张对比）

## 7. 执行备注（给 Gemini 3.8 Flash）

- 工作目录即本仓库；代码放 app/，脚本放 scripts/，文档放 docs/
- 严格按 P0 → P1 → P2 → P3 顺序推进，P0 判定结果决定后续走向，若需降级路线先停下来在交付里说明
- 每完成一个里程碑，把证据（截图/日志/录屏路径）写进 docs/progress.md
- 不安装全局依赖；Python 依赖用项目内 venv
- 本机为 macOS (darwin)，无 shell timeout 命令，不要用
